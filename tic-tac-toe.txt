Cloned the assign3 repository. Encountered an issue with ./helper wsl not working, but 
realized I was in the wrong direcatory. I went to assign3 directory and successfully ran sudo
./helper wsl and ./helper init to set up the local environment and install dependencies.

Replaced the starter code in App.jsx with the starting Square component from the React tutorial.

Converted the Square function to a Board function with 3x3 squares each labelled 1-9.

Introduced the square function again, this time with the functionality of clicking resulting in
an 'X' appearing in the box. Also removed the 1-9 numbering so the grid starts empty.

Square now takes on value from Board and the 3x3 display remain empty even when clicked.

Updated the Square components to accept an onSquareClick prop, allowing the parent Board 
component to centrally manage state updates via a handleClick function. Using arrow 
functions to pass arguments to the event handler without triggering an infinite re-render loop.

Added xIsNext state boolean to the Board component to track whose turn it is and alternate 
between 'X' and 'O'. Updated the handleClick function to return early if a square already 
contains a value, preventing players from overwriting existing moves.

Added a calculateWinner helper function to evaluate all possible winning lines on the board. 
Updated the handleClick function to prevent further moves if a winner is found and added a 
status element to the UI to display either the next player turn or final winner.